# 変更履歴

## 未リリース

### 修正
- YouTube の認証トークンが設定ファイルと別の場所に保存されていた問題を修正
  - 存在しないメソッドを呼んでおり，その例外を握りつぶして
    常に `~/.twitchTransFreeNeo/` へ保存していた
  - 設定ファイルと同じ場所に統一（旧保存先のトークンは自動で引き継ぐため再認証は不要）
- 開発時に残っていたデバッグ出力（15箇所）を削除
- `.gitignore` に認証トークンを追加

### ドキュメント
- README を実態に合わせて修正
  - YouTube Live 対応を明記（従来は Twitch のみと書かれていた）
  - 必要環境の記述を修正（tkinter は使っていない／Python 3.10〜3.12）
  - ダウンロードするファイル名を実際のリリース名に修正
  - 実行手順を uv / pip の両方で記載
- `requirements.txt` を実際の依存関係に更新
  - `flet` が入っておらず，記載どおりの手順ではソースから起動できなかった
  - 使っていない `async-google-trans-new` `playsound` を削除
  - クリーンな環境で `pip install -r requirements.txt` が通ることを確認済み

### 重要な修正（外部レビューで判明）
- **DeepL を選んでも実際には Google 翻訳が使われていた問題を修正**
  - 依存していた `deepl-translate` に `Translator` クラスが存在せず，
    初期化が必ず失敗 → 例外を握りつぶして Google へ切り替わっていた
  - 利用者が意図しない送信先へチャットが送られる状態だったため，
    公式の `deepl` SDK に切り替え
  - DeepL が使えない場合は Google へ勝手に切り替えず，理由を明示して翻訳を止めるようにした
- **翻訳キャッシュが翻訳エンジンを区別していなかった問題を修正**
  - Google から DeepL に切り替えても，以前の Google の訳が返っていた
  - エンジンごとに分けて保存する（既存のキャッシュは Google のものとして自動で引き継ぎ）
- **正式版タグでも「Pre-release」として公開される問題を修正**
  - タグ名に Beta / rc が含まれる場合だけ事前公開扱いにする

### 安定性の改善（外部レビューを受けて）
- 読み上げ待ちの上限を設けた（50件）
  - 配信が盛り上がってコメントが殺到しても、読み上げが延々と遅れて
    溜まり続けることがなくなりました（あふれた分は古い順に省略します）
- 音声合成の通信にタイムアウトを設定（通信不良で待ち続けるのを防止）
- 読み上げの停止時に、再生中の音を止めてスレッドの終了を待つようにした
- **接続ボタンの連打で二重に接続されてしまう問題を修正**
  - 処理中はボタンを押せないようにし、状態を表示するようにしました
- 接続停止時の後始末を修正
  - 内部状態を壊す操作をやめ、終了を待ってから完了とするようにしました
  - 停止処理で画面が固まることがあったのも解消しています
- YouTube の監視が止まったときに「接続中」の表示が残る問題を修正
  - 配信終了などを検出したら表示を未接続へ戻します
  - 取得エラーが続く場合は待ち時間を延ばして再試行します

### 修正
- **「表示のみモード」が接続できなかった問題を修正**
  - Twitch はチャットを読むだけでも認証が必要なため，ダミーのトークンでは
    必ず認証エラーになっていました
  - トークン未設定のときは接続を試みる前に案内を出すようにしました
- 設定ファイルの保存を安全な方式に変更
  - 書き込み中に終了しても設定（トークンを含む）が失われないよう，
    一時ファイルに書いてから置き換えます
  - 認証情報を含むため本人だけが読める権限で保存します
- 翻訳キャッシュの自動整理を有効化
  - 削除処理は用意されていたものの，どこからも呼ばれておらず
    データベースが増え続ける状態でした（60日より古いものを起動時に削除）

### 新機能
- **読み上げ速度の変更に対応**（0.5倍〜2.5倍・既定は1.4倍）
  - 設定 →「TTS」→「TTS詳細設定」のスライダーで変更できます
  - 追加のソフトを入れる必要はありません（音声を内部で伸縮しています）
  - 音の高さは変わらないため，速くしても聞き取りやすさを保てます
- **長い発言を自動で速く読む機能**（既定で有効・設定で切り替え可）
  - 30文字までは基準の速度、90文字で最大まで加速します
  - 長いコメントの読み上げが延々と続き，次のコメントに追いつかなくなるのを防ぎます
  - 基準速度を速く設定していても 2.5倍を超えないよう頭打ちにしています

### 設定が効かなかった項目の修正
- **フォントサイズの設定がどこにも適用されていなかった問題を修正**
  - 設定画面で変更しても，再起動しても表示は変わらないままだった
  - チャットのユーザー名・原文・翻訳文に反映し，「適用」で即座に変わるようにした
- **アクセントカラーの設定が適用されていなかった問題を修正**
  - 同じく設定項目があるだけで機能していなかった
  - 選択した色をテーマに反映し，起動時にも復元するようにした
- ウィンドウサイズの変更を「適用」で即座に反映するようにした（従来は再起動が必要）
- 設定画面のウィンドウサイズ欄に，設定値ではなく現在の実際のサイズを表示するようにした
- 「変更は再起動後に反映されます」の注記を実態（即時反映）に合わせて修正

### UI の改善
- チャット一覧の情報密度を上げた（1件あたり約123px → 約73px）
  - メッセージを囲む枠をやめ，下線だけの区切りにした
  - 余白と行間を詰め，同じ画面に見えるメッセージ数を約1.7倍にした
- 操作アイコン（ピン留め・お気に入り・コピー）をカーソルを載せたときだけ表示するようにした
  - 従来は1件あたり4つ常時表示され，一覧が見づらくなっていた
- 翻訳行の先頭にあった翻訳アイコンをやめ，青い縦線と文字色で原文と区別するようにした
- チャットが1件も無いときに使い方の案内を表示するようにした
  （未接続なら「接続開始」への案内，接続中なら待機中である旨）
- 右側のログ枠が画面下まで伸びて大きな空白になっていたのを，高さを抑えて下端に配置した
- 設定画面がウィンドウの大きさを活かすようにした
  - 従来は中身の表示領域が固定（720×520）で，画面を大きくしても中身だけ狭いままだった
  - 大きなウィンドウでは一度に見える設定項目が2枚→4枚に増えた
- 設定画面の入力欄を，幅に余裕があれば横に並ぶようにした（狭い場合は自動で折り返す）
  - フィルタ設定はスクロールなしで全項目が見えるようになった
- 「↓ 下にスクロールで続きます」の帯を廃止（表示領域を広げたため）
- TTS設定の「最大文字数」のラベルが折り返して欠けていたのを修正

## v0.2.28_Beta - 2026-08-26

### 重要な修正
- **翻訳結果に「Error 500 (Server Error)」が投稿される不具合を修正**
  - 原因: deep-translator が利用する `translate.google.com/m` が 2026-08-24 頃から
    間欠的にエラーページを返すようになり，その本文が翻訳結果として通っていた
  - 翻訳エンドポイントを多段化（clients5 → translate.googleapis.com → deep-translator）
  - エラーページ文面を検出し，翻訳結果として採用しない
  - エラー文面をデータベースへ保存しない（保存済みの行は起動時に自動削除）
    ※ 旧版はエラー文をキャッシュしていたため，Google が復旧しても
      同じ発言に対してエラー文を出し続ける状態になっていた
- **言語検出が機能していなかった不具合を修正**
  - 原因: `single_detection(text, api_key=None)` は API キー必須のため必ず例外になり，
    常に簡易ヒューリスティクスへ落ちていた
  - 影響: スペイン語・ポルトガル語・フランス語などラテン文字の言語が
    すべて英語と誤判定されていた（ウクライナ語→ロシア語，ペルシャ語→アラビア語も同様）
  - かな・ハングルのように文字種で確定できるものはローカル判定，
    それ以外はオンライン検出に変更
- **翻訳キャッシュが保存されていなかった不具合を修正**
  - データベースが相対パスだったため，アプリ起動時のカレントディレクトリによっては
    書き込みに失敗していた（設定ファイルと同じ場所に保存するよう統一）
- **システム診断が翻訳障害を検出できなかった問題を修正**
  - 以前は translate.google.co.jp にアクセスして 200 が返れば「正常」としていたため，
    翻訳APIが障害中でも「正常」と表示されていた
  - 実際に翻訳を実行し，結果を検証するよう変更
  - 診断処理が2箇所に重複しており，画面から使われていない側だけを直していたのを是正
    （未使用だった `utils/diagnostics.py` は削除）

### その他の修正
- 起動時の挨拶メッセージが二重に投稿されることがある問題を修正
- 翻訳文が長い場合に Twitch へ投稿できなかった問題を修正（500文字で切り詰め）
- 読み上げ文が二重に短縮され，ユーザー名が長いと本文がほとんど読まれなかった問題を修正
- 読み上げ対象言語の指定が入力言語だけで判定されていた問題を修正
  （「日本語だけ読み上げる」設定で翻訳文が読まれなかった）
- gTTS が対応していない言語コード（iw/jw/地域バリアント等）を変換するよう修正
- 読み上げ停止時に未読み上げのメッセージを破棄するよう修正（再開時に古い発言が読まれていた）
- 接続に失敗しても「接続中」と表示され続ける問題を修正
  （認証エラーの理由をログに表示し，表示を未接続へ戻す）
- テーマ（ライト/ダーク）の切り替えが保存されず，再起動で戻る問題を修正
  （システムテーマのまま切り替えても見た目が変わらない場合があったのも修正）
- ウィンドウサイズを変更しても次回起動時に元に戻る問題を修正

### 改善
- チャット表示を差分更新に変更（従来はメッセージ1件ごとに100件分を再描画していた）
- 言語検出時に取得した訳文を再利用し，翻訳APIの呼び出し回数を削減

## v0.1.2_Beta - 2025-01-13

### 変更内容
- プロジェクト構造の整理
  - twitchTransFreeNextフォルダとその内容を削除（参照用のコピーだったため不要）
  - 開発ドキュメントをdocs/フォルダに移動
    - CLAUDE.md
    - GITHUB_ACTIONS_SETUP_PLAN.md
    - setup_instructions.md
    - twitchTransFreeNext_詳細分析レポート.md
  - tmpフォルダと古い音声ファイルを削除
- ルートフォルダをクリーンアップして見通しを改善

## v0.1.1_Beta - 2025-01-13

### 変更内容
- テスト用ファイルの削除
  - minimal_test.py, simple_gui.py, standalone_gui.py, test_gui.py, test_startup.py
  - test_config.json, test_translations.db
  - GUI内のテスト関連ファイル（simple_chat_display.py, simple_main_window.py, simple_settings.py, main_window_backup.py）
- README.mdの開発者情報を修正
  - 開発者がsayonari (@husband_sayonari_omega)であることを明記
  - twitchTransFreeNextも同一開発者によるものであることを明記
  - 協力者として妻のさぁたんを記載

## v0.1.0_Beta - 2025-01-12

### 初回リリース
- twitchTransFreeNeoの初回ベータ版リリース
- CUI版twitchTransFreeNextをGUI化
- tkinterによる直感的なインターフェース
- リアルタイム翻訳機能
- 設定画面の実装
- 翻訳履歴の保存機能